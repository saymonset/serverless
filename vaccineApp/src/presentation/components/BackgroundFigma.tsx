import React from 'react'
import { View, Platform } from 'react-native'

export const BackgroundFigma = () => {
    return (
        <View 
            style={{
                position: 'absolute',
                backgroundColor: '#80BFFF',
                width: 1000,
                height: 1200,
            }}
        />
    )
}
